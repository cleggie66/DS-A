// Create object
// Iterate through both strings
// Check if string 1 at index is a key in object
// If it is, check if value matches string 2 at index
// If it does not match, exit with false
// If it does match, continue
// If key doesn't exist, add string1 at index as a key, and string 2 at index as the value
// If done iterating, exit with true